import * as React from 'react';
import { Text } from 'react-native';

export interface IDirectionsProps {
}

const Directions = (props: IDirectionsProps) => {
  return (  
    <Text>HELLO WORLD FROM DIRECTIONS</Text>
  );
}

export default Directions;